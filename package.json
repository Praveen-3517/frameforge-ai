{
  "name": "frameforge-ai-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm run dev:backend",
    "dev:backend": "cd backend && .\\.venv\\Scripts\\uvicorn.exe main:app --reload --port 8005",
    "build": "npm run build --prefix frontend"
  }
}
